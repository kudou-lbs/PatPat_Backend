<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.games.tap.mapper.LoginMapper">
    <select id="getUserLoginByName" resultType="com.games.tap.domain.UserLogin">
        SELECT * FROM user_login WHERE username=#{userName}
    </select>

    <select id="getUserLoginByUId" resultType="com.games.tap.domain.UserLogin">
        SELECT * FROM user_login WHERE uid=#{uId}
    </select>

    <cache readOnly="true"/>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.games.tap.mapper.LoginMapper">

    <insert id="insert">
        insert into user_login(password, username, uid) VALUES (#{password},#{userName},#{uId})
    </insert>

    <update id="update">
        update user_login set password=#{password} where id=#{id}
    </update>

    <delete id="deleteById">
        delete from user_login where id=#{id}
    </delete>

    <select id="getById" resultType="com.games.tap.domain.UserLogin">
        select * from user_login where id=#{id}
    </select>

    <select id="login" resultType="com.games.tap.domain.UserLogin">
        select * from user_login where username=#{username} and password=#{password}
    </select>


</mapper>
package com.games.tap.controller;

import io.swagger.v3.oas.annotations.tags.Tag;
import org.springframework.stereotype.Controller;

@Controller
@Tag(name = "task",description = "包含定期执行任务的接口，功能待定")
public class TaskController {
}
